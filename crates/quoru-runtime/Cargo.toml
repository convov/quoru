[package]
name = "quoru-runtime"
version = "0.1.0"
edition = "2021"
description = "Quoru daemon — hosts durable multi-agent conversations on Fuse."
license = "BSL-1.1"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
