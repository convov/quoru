[package]
name = "quoru-sdk"
version = "0.1.0"
edition = "2021"
description = "Quoru pack-author SDK — manifest types, worker registration, conversation client."
license = "BSL-1.1"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
toml = "0.8"
